---
const { title, desc, id="" } = Astro.props;
---

<div class="header" id={id}>
    <h1>{title}</h1>
    <p>{desc}</p>
</div>

<style>
    h1 {
        color: var(--primary);
    }

    h1, p {
        text-align: center;
    }

    .header {
        margin: 6rem;
    }

</style>